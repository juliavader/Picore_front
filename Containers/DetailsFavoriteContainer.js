import { connect } from 'react-redux'
import DetailsFavoriteApp from "../Screen/DetailsFavoriteApp";


function mapStateToProps(state) {
    return state
}

function mapDispatchToProps(dispatch) {
    return({
        
    })
}


export default connect(mapStateToProps)(DetailsFavoriteApp);
