import { connect } from 'react-redux'
import FavoriteApp from "../Screen/FavoriteApp";


function mapStateToProps(state) {
    return state
}

function mapDispatchToProps(dispatch) {
    return({
        
    })
}


export default connect(mapStateToProps)(FavoriteApp);
